cloud:
  aws:
    credentials:
      access-key: ${ACCESS-KEY}
      secret-key: ${SECRET-KEY}
    region:
      static: us-east-1
    stack:
      auto: false

application:
  bucket:
    name: myfirstuser

server:
  port: 9090